# POL Pump Gaming Backend - Environment Configuration
# Copy this file to .env and update with your values
# DO NOT commit .env file with real secrets

# ====================
# Server Configuration
# ====================

# Server port (default: 4000)
# Priority: BACKEND_PORT > PORT > 4000
BACKEND_PORT=4000

# Frontend origin for CORS (used in production)
# Default: http://localhost:3000 (development)
FRONTEND_ORIGIN=http://localhost:3000

# Legacy: NEXT_PUBLIC_BACKEND_URL (kept for compatibility)
# This is used by the frontend, do not remove
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# ====================
# Blockchain/RPC Configuration
# ====================

# RPC URL for blockchain queries
# Priority: RPC_URL > NEXT_PUBLIC_EVM_RPC > POLYGON_AMOY_RPC > default
RPC_URL=https://polygon-amoy.infura.io/v3/b4f237515b084d4bad4e5de070b0452f

# Alternative RPC env vars (for compatibility with existing code)
NEXT_PUBLIC_EVM_RPC=https://polygon-amoy.infura.io/v3/b4f237515b084d4bad4e5de070b0452f
POLYGON_AMOY_RPC=https://polygon-amoy.infura.io/v3/b4f237515b084d4bad4e5de070b0452f

# Enable/disable on-chain validation (default: true)
# Set to 'false' to disable RPC calls entirely (faster, but no validation)
ENABLE_ON_CHAIN_VALIDATION=true

# ====================
# Node Environment
# ====================

# Set to 'production' for production builds
NODE_ENV=development

# ====================
# Notes
# ====================
# - DO NOT commit .env file with real secrets
# - All RPC URLs above are testnet endpoints (safe to commit)
# - For production, use a private RPC endpoint or API key
# - Database files are stored in ../data/ (project root)


